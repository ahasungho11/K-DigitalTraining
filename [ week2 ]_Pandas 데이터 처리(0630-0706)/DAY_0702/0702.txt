0702
<생각하며 듣기>
오전1)
1. 과제 피드백
- 프로젝트 단위에서는 상대경로로 하는 경우가 많지 (익숙해지도록)
- 데이터들(텍스트 등 ) / 이미지 데이터들 => 종류에 따라 폴더 구분하는 것이 좋음
- 오픈카톡, 질문
- 암묵적으로 코드 상 대문자로 쓰면 정해진 것이 있는 (like 고유명사) 상수를 표시함
- .columns로 컬럼명 불러서 index와 함께 쓰기도

1) iris 문제
(1) 직접 뽑기
(2) 슬라이싱
(3) list comprehension 사용
 ex) irisDF[col for col in irisDF.columns if in 'Length' in col]
 => 하드 코드 자체는 지양할 것. 2)나 3)에 익숙해지도록

- warning과 error은 다른 것
 -> 붉은 색깔로 나오면서 경고주는 것임 (슬라이싱 쓸 경우, 多)
 -> [ .copy() ] 를 입력하면 경고창이 사라짐
 - DF(시리즈 , 시리즈, index) 이렇게 DF를 만들어도 되니까 => 캡처有
  (001, 002)

- describe()는 수치 date의 기초통계를 나타내 줌, 문자열은x
 -> 문자열의 경우, 빈도수 등 다른 내용이 나올 수 있으니, 타입을 바꿔주면 될 것임

- 판다스 형변환 -> DF/SR.astype(변경하고자 하는 타입)

2) 전력문제
- read_xxx 의 인자들
- 비어있는 데이터는 어떻게 할 것인가.
- 연도에 적혀있는 수치들이 순수 데이터라 볼 수 있지
 -> 데이터에 '-'가 있넹 (비어있는 것과는 다름, 남한이나 북한 밑에 빈칸과는 다른 것)
- .read_excel(FILE_NAME, na_values='-', na_filter=True)
 -> '-'가 들어 있으면 NaN으로 처리하겠다
 -> head()로 상태 확인할 것

 최대, 최소값 문제(003)
- 컬럼에 뭐가 있지? -> DF.columns
 -> 리스트 나오지 
- 결과값이 나오면 type을 출력해보라 => describe()는 DF를 출력함
 -> 특정 행, 열을 뽑을 수 있다는 것(004)
 -> 리스트명, 범위 지정(슬라이싱), 타입확인, 조건에 맞는 행이나 열 뽑기

짝수 컬럼 뽑기
- 컬럼명만 들어 있는 변수 하나를 만들어서 활용하는 것이 편함
- yearCols.columns[::2]    (year은 연도만 죽 뽑아 놓은 컬럼명들을 담아 놓은 것임)

컬럼 합치기+다음문제 (006)
- 합쳐서 추가한 컬럼이 제일 끝에 나오니까 -> set_index()하면 젤 앞으로 오고
- 이후 2개를 .drop()해버리면 됨
- na_filter 등 말씀해 주신 것들 제일 앞에서만 활용하는 건 아니니 캡쳐본 보고 익힐 것

파일을 열었을 때, 앞쪽에 필요한 데이터가 아닌 경우, skip할 여부도 결정해주고

2. 수업 시작
# 147
# 165~

오전2)
- 주피터 노트북 필기한 것
@ PPT 파일 찬찬히 죽 읽어 볼 것
- 살짝 실습

오후1)
- 다른 분들 것도 조금 봐주고 (flow)
- pandas 사이트- 파라미터들 봐주기 (seires 등)

# 168~
- 하~ 교재 한번 살펴봐도 도움될 것 같음 (해석 skill)
- 편차의 크기 확인 -> 평균의 효용성 확인 -> 
- 각 용어마다 사용하는 case를 확인해봐야

- 두 '열'. 즉, 두개의 '컬럼'에 대한
 -> 가설 및 주제 등과 가장 가까운 것 같은 컬럼을 골라서 일단 우선적으로 확인
 -> 

- 전처리 (# 173~)
 -> 데이터 처리-> 변환 -> 크기
 -> 누락 데이터 여부 체크 (함수 활용)
 -> 누락 데이터 개수 체크
 -> 누락 데이터 제거 / 치환
 -> 

오후2)
- 첫 줄에 컬럼명이 있네, 구분자가 콤마(,)네. 오케이-
- DataFrame.dropna(axis=0, how='any', thresh=None, subset=None, inplace=False)
 => https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.dropna.html
 => 각 파라미터가 의미하는 바 익히기
 ex) any < thresh < how
      row를 제거하는 데 subset(확인할 컬럼 묶는 것)

오후3)
- 학습했던 NaN 처리 방법들 큰 틀로 정리해서 숙지하도록

- 과제 설명
 -> Main : 코로나가 학교에 미친 영향 분석
 -> Sub : 코로나와 유치원(1)/초등(1)/중등(1)/고등(1)/대학교(1) 등 이런식,,
 -> 공공데이터 포털 (ask 확인), 한국관광 데이터랩
 -> 빅데이터 자료 다운받을 수 있는 site
 -> 웹을 연동해서 쓸 때 API를 쓸테고, 현재는 file data를 다운받도록
 -> '무엇'을 분석하겠다 가 있어야 함


=========================================
- PPT, 주피터 노트북, 메모장
- 전반적 과정 (항목 항목 마다 명칭 적어가면서)
- 기릿 등(국제, 사회, 경제 전반)
-  파라미터 보면서 익히기 (site)
- Frame
1. 모듈 로딩
2. 파일 관련 변수 선언
3. CSV File (불러올 파일) -> DataFrame으로 로딩
 -> 첫줄이 컬럼명으로 되어 있는지
 -> 구분자들이 확장자와 맞게 사용되었는지
 -> 결측치(Missing Value)의 정도
4. 데이터 정보 확인
 -> .info() / .head() / .tail() / .shape / .columns / index
 -> 변수명, 표의 구성 등으로 어떤 내용인지 파악
 -> 결측치(NaN 등)의 양으로 판단하여,
     유지하는 것이 맞는가 vs. 치환하는 것이 맞는가
 -> 


