6/27
csv -> 콤마(,)로 데이터 구분 => 콤마 빼고 내용 빼내지
(콤마로만 되어 있지는 않으니, 열어서 살펴봐야함)
표준 모듈 있음 -> 일일이 스트링 구분 할 필요는 없음
import csv

JASON
[ { ~ }, { ~ } ]

import JASON

편하게 다룰 수 있는 모듈이 있는지 확인해서 활용
excel, xml, JASON, csv 등

======================================

#1-#11
stack을 byte로 쪼개
(byte-1개의 data가 들어가는 가장 작은 단위)
(0-256)
short -2 
int - 4
=> 이런식으로 type에 맞는 할당된 메모리 정해져 있음


heap 
동적할당
구조체 - structure
발생하는 변수들을 묶어서 공간 할당

각종 정의한 함수들도 같은 data를 위함이라면 한꺼번에 묶어
- 수정, 업데이트도 이 묶은 덩어리만
- 그 묶은 덩어리를 class라고 함 (변수와 함수 등, 일종의 type)
- 변수에다가 함수가 들어가서 구조체의 확장으로 보기도 함
- 데이터를 저장하는 변수와 함수를 다 묶은 것
- 변수만 / 메서드만 / 변수와 메서드 모두 / 모두x => 형태는 다양함 (like 함수 종류)


유지, 보수, 수정이 쉬움 (해당 클래스만 손보면 다른 곳에는 다 적용 된다는 것)

함수 - 클래스 -> 안에 들어 있는 함수를 '메서드'
데이터 - field, attribute 등

data 저장 타입 -> class
heap에 class 저장
변수를 만들어 heap에 있는 class를 (저장공간이 달라서) 참조함

#68
문자, 숫자, 실수 등 파이썬에서는 class로 묶어서 제공해주고 있는 것


