6/23
1. 파이썬 실습 (주석으로 수업내용 적은 것)
2. PPT (다 볼 때까지만 해당 내용 보면 될 듯)
3. 메모장에 작성한 것 보기
4. 점투파 일부 필기해 놓은것 봐주기

return
- 마지막, 다음줄에 적어봤자 이미 돌아간 상태니 x
- break 와도 함께 사용하는 경우도 

p152
p177

return 값이 없으면 none이 출력되지

함수 - 매개변수, 인수, 호출 => 정의 
함수 종류
매개변수 초기값(default value) 

#195~ (함수)

- 함수 종료 및 호출한 곳으로 돌아감
- 어떤 종류의 객체도 반환 가능
- return 사용하지 않거나 return만 적어도 함수 종료
- 함수 중간에 빠져나가기 (break)
- 여러 개 값 반환 가능 (소괄호 제외하면 튜플로 쓸 수 있으니, 파이썬은 여러개 반환가능)

주피터 노트북이나 코랩(구글 클라우드 서비스)을 실무에서 많이 쓰게 될 것임

- 변수에 함수 담는 것도 가능
def plus(a,b):
    return a*b

def minus(a,b):
    return a-b

first=[plus, minus]
print(first[0](1,2))  ->  2
print(first[1](1,2))  ->  -1

@@ 자료구조, 알고리즘 학습 도서  =>  봐보기

- 매개변수가 없어도, 함수임을 나타내기 위해서는 괄호()를 적어주어야함

- return A 라면
-> 함수()로 부른 그 자리에 A를 넣어주는 것

-> 함수() <=> A

- 함수의 매개변수에 대한 설명을 넣고자 할 때는
함수():
'''   까지 입력해주면 설명에 쓰일 기본적인 양식이 나오고,
해당하는 곳에 입력해주면 됨
=> 함수에 마우스를 갖다대면 설명 나옴
''' => doc str
(이렇게 설명하는 것도 양식은 회바회. 대부분 제일 윗부분에 적고 시작함)
hisotry로 남김
ex)
HISTORY : 2022-06-23 HEO create
	 2022-06-25 CHOI modify

- 개발환경 구축 (공통된 양식 공유 포함 등등)

file > settings > pfoject : 이름 > python interpreter > show all 해서 추가 등 편집 

<가변인자>
- 매개변수 0개~ n개
- list, tuple, dictionary, set (나열할 수 있는)
 -> 매개변수는 변경없는 상태로 받아야 하니까, tuple로 전달됨

변수명 = lamda 인수1, 인수2, ... : 실행코드 
             ( 리턴 값으로 바뀌어 '변수명'으로 들어가는 것 )

- 함수로 저장해놓고 쓰지는 않지만, 해당 부분만 잠시 쓰고 치우겠다는 경우
- 함수의 인자로 lambda를 쓰는 경우도 있음




텍스트 vs. 바이너리
txt, csv, 

- 해당 데이터가 텍스트로 이뤄져 있는지 2진수로 이뤄져 있는지 판단해야함
- 인코딩과 디코딩을 할 때 같은 테이블에 두고 행해야 깨지지 않음
- 


Q. 파일 열기/읽기/저장하기/닫기

입출력, 반복문
파일 추가 (생성)
항목 추가할 때, 즉각적으로 리스트에 넣기
day
addressbook

메뉴 보여주기 (while) - 없는 번호 누를 경우 예외 조건 넣어주고
1-3 메뉴 보여주기
4 종료 빠져나가기

각 기능 넣어주기
3 입력받아 파일 만들고 (1개 디렉토리 안에 10개정도 들어가게)

==========
이름.py  로 구글 드라이브에 업로드
==========
앞판 사진 넣어서 다시 보내기 (지원서)
